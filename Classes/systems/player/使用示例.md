# Player 系统使用示例

## 完整集成示例

### 场景代码示例

```cpp
// YourGameScene.h
#ifndef __YOUR_GAME_SCENE_H__
#define __YOUR_GAME_SCENE_H__

#include "cocos2d.h"
#include "entt/entt.hpp"

class YourGameScene : public cocos2d::Layer {
public:
    static cocos2d::Scene* createScene();
    virtual bool init();
    virtual void update(float delta) override;

    CREATE_FUNC(YourGameScene);

private:
    entt::registry _registry;       // EnTT 注册表
    entt::entity _playerEntity;     // 玩家实体

    void createPlayer();
    void updatePlayer(float dt);
    void updatePlayerUI(float dt);
};

#endif
```

```cpp
// YourGameScene.cpp
#include "YourGameScene.h"
#include "player/PlayerFactory.h"
#include "player/PlayerComponents.h"
#include "player/PlayerInput.h"

USING_NS_CC;

Scene* YourGameScene::createScene() {
    // 创建带物理引擎的场景
    auto scene = Scene::createWithPhysics();

    // 设置重力
    auto physicsWorld = scene->getPhysicsWorld();
    physicsWorld->setGravity(Vec2(0, -980));

    // 可选：启用物理调试绘制
    // physicsWorld->setDebugDrawMask(PhysicsWorld::DEBUGDRAW_ALL);

    auto layer = YourGameScene::create();
    scene->addChild(layer);
    return scene;
}

bool YourGameScene::init() {
    if (!Layer::init()) {
        return false;
    }

    auto visibleSize = Director::getInstance()->getVisibleSize();
    Vec2 origin = Director::getInstance()->getVisibleOrigin();

    // 创建背景
    auto background = LayerColor::create(Color4B(135, 206, 235, 255));
    this->addChild(background, 0);

    // 创建地面（简单示例）
    auto ground = Sprite::create();
    ground->setTextureRect(Rect(0, 0, visibleSize.width, 40));
    ground->setColor(Color3B(100, 150, 100));
    ground->setPosition(Vec2(visibleSize.width / 2, 20));
    this->addChild(ground, 0);

    auto groundBody = PhysicsBody::createBox(ground->getContentSize());
    groundBody->setDynamic(false);
    ground->setPhysicsBody(groundBody);

    // 初始化输入系统
    PlayerInput::getInstance().initialize(this->getScene());

    // 创建玩家
    createPlayer();

    // 启动更新
    this->scheduleUpdate();

    return true;
}

void YourGameScene::createPlayer() {
    auto visibleSize = Director::getInstance()->getVisibleSize();

    // 使用 PlayerFactory 创建玩家
    Vec2 spawnPos(visibleSize.width / 2, 300);
    _playerEntity = PlayerFactory::createPlayer(_registry, spawnPos, this);

    CCLOG("Player created in scene!");
}

void YourGameScene::update(float delta) {
    // 1. 更新输入状态
    PlayerInput::getInstance().update(delta);

    // 2. 更新玩家逻辑
    updatePlayer(delta);

    // 3. 更新玩家UI
    updatePlayerUI(delta);
}

void YourGameScene::updatePlayer(float dt) {
    auto& input = PlayerInput::getInstance();

    // 遍历所有玩家（通常只有一个）
    auto view = _registry.view<ecs::PlayerTag,
                                ecs::PlayerMovementComponent,
                                ecs::PlayerStatsComponent,
                                ecs::TransformComponent,
                                ecs::PlayerSpriteComponent>();

    for (auto entity : view) {
        auto [movement, stats, transform, sprite] =
            view.get<ecs::PlayerMovementComponent,
                     ecs::PlayerStatsComponent,
                     ecs::TransformComponent,
                     ecs::PlayerSpriteComponent>(entity);

        // === 处理输入 ===
        movement.isMovingLeft = input.isActionPressed("MoveLeft");
        movement.isMovingRight = input.isActionPressed("MoveRight");
        movement.wantsToJump = input.isActionJustPressed("Jump");

        // === 移动逻辑 ===
        float targetVelX = 0.0f;
        if (movement.isMovingRight) {
            targetVelX = stats.moveSpeed;
            movement.isFacingRight = true;
        }
        if (movement.isMovingLeft) {
            targetVelX = -stats.moveSpeed;
            movement.isFacingRight = false;
        }

        // 平滑加速
        float accel = movement.accelerationRate * dt;
        if (targetVelX != 0) {
            movement.velocity.x += (targetVelX - movement.velocity.x) * accel * 0.1f;
        } else {
            // 摩擦力减速
            movement.velocity.x *= (stats.isOnGround ? movement.friction : movement.airResistance);
        }

        // 限制最大速度
        if (std::abs(movement.velocity.x) > movement.maxHorizontalSpeed) {
            movement.velocity.x = (movement.velocity.x > 0)
                ? movement.maxHorizontalSpeed
                : -movement.maxHorizontalSpeed;
        }

        // === 跳跃逻辑 ===
        if (movement.wantsToJump && stats.isOnGround) {
            movement.velocity.y = stats.jumpHeight;
            stats.isOnGround = false;
        }

        // === 同步到物理引擎 ===
        if (sprite.sprite && sprite.sprite->getPhysicsBody()) {
            auto body = sprite.sprite->getPhysicsBody();
            body->setVelocity(Vec2(movement.velocity.x, body->getVelocity().y));

            // 更新位置
            transform.x = sprite.sprite->getPositionX();
            transform.y = sprite.sprite->getPositionY();

            // 检测地面（简单检测）
            if (body->getVelocity().y <= 0 && transform.y <= 100) {
                stats.isOnGround = true;
            }
        }

        // === 翻转精灵 ===
        if (sprite.sprite) {
            sprite.sprite->setFlippedX(!movement.isFacingRight);
        }
    }
}

void YourGameScene::updatePlayerUI(float dt) {
    auto view = _registry.view<ecs::PlayerTag,
                                ecs::PlayerStatsComponent,
                                ecs::PlayerSpriteComponent>();

    for (auto entity : view) {
        auto [stats, sprite] = view.get<ecs::PlayerStatsComponent,
                                        ecs::PlayerSpriteComponent>(entity);

        // 更新血条
        if (sprite.healthBarFill) {
            float healthPercent = stats.currentHealth / stats.maxHealth;
            sprite.healthBarFill->setScaleX(healthPercent);
        }

        // 更新魔法条
        if (sprite.manaBarFill) {
            float manaPercent = stats.currentMana / stats.maxMana;
            sprite.manaBarFill->setScaleX(manaPercent);
        }
    }
}
```

## 快速集成步骤

### 步骤1：在 CMakeLists.txt 中添加 Player 文件

```cmake
list(APPEND GAME_SOURCE
     # ... 现有文件
     Classes/player/PlayerInput.cpp
     Classes/player/PlayerFactory.cpp
)

list(APPEND GAME_HEADER
     # ... 现有文件
     Classes/player/PlayerComponents.h
     Classes/player/PlayerInput.h
     Classes/player/PlayerFactory.h
)
```

### 步骤2：在场景头文件中添加

```cpp
#include "entt/entt.hpp"

private:
    entt::registry _registry;
    entt::entity _playerEntity;
```

### 步骤3：在场景初始化中

```cpp
// 确保场景使用物理引擎
auto scene = Scene::createWithPhysics();
scene->getPhysicsWorld()->setGravity(Vec2(0, -980));

// 初始化输入
PlayerInput::getInstance().initialize(scene);

// 创建玩家
_playerEntity = PlayerFactory::createPlayer(_registry, spawnPos, this);
```

### 步骤4：在 update 中更新

```cpp
void update(float dt) override {
    PlayerInput::getInstance().update(dt);
    // 你的玩家更新逻辑...
}
```

## 高级用法

### 1. 修改玩家属性

```cpp
auto& stats = _registry.get<ecs::PlayerStatsComponent>(_playerEntity);
stats.maxHealth = 150.0f;
stats.currentHealth = 150.0f;
stats.moveSpeed = 250.0f;
```

### 2. 装备物品

```cpp
auto& equipment = _registry.get<ecs::PlayerEquipmentComponent>(_playerEntity);
equipment.helmet.itemId = 1101; // 铜头盔
equipment.chestplate.itemId = 1102; // 铜胸甲
```

### 3. 添加Buff

```cpp
auto& buffComp = _registry.get<ecs::PlayerBuffComponent>(_playerEntity);
ecs::PlayerBuffComponent::Buff speedBuff(1, 10.0f);
speedBuff.moveSpeedMultiplier = 1.5f; // 速度+50%
buffComp.addBuff(speedBuff);
```

### 4. 使用快捷栏

```cpp
auto& hotbar = _registry.get<ecs::PlayerHotbarComponent>(_playerEntity);
int currentSlot = hotbar.selectedIndex;
int inventoryIndex = hotbar.slots[currentSlot];

// 切换槽位
hotbar.selectNext();  // 下一个
hotbar.selectPrevious();  // 上一个
hotbar.selectSlot(5);  // 直接选择第6格
```

### 5. 检测玩家状态

```cpp
auto& stats = _registry.get<ecs::PlayerStatsComponent>(_playerEntity);

if (stats.isOnGround) {
    CCLOG("Player is on ground");
}

if (stats.currentHealth <= 0) {
    CCLOG("Player is dead!");
    // 处理死亡...
}
```

## 调试技巧

### 显示调试信息

```cpp
// 在场景中添加调试标签
auto debugLabel = Label::createWithSystemFont("", "Arial", 14);
debugLabel->setPosition(Vec2(100, 500));
this->addChild(debugLabel, 1000);

// 在 update 中更新
this->schedule([this, debugLabel](float dt) {
    auto& stats = _registry.get<ecs::PlayerStatsComponent>(_playerEntity);
    auto& movement = _registry.get<ecs::PlayerMovementComponent>(_playerEntity);
    auto& transform = _registry.get<ecs::TransformComponent>(_playerEntity);

    std::string info = StringUtils::format(
        "Position: (%.0f, %.0f)\n"
        "Velocity: (%.1f, %.1f)\n"
        "HP: %.0f/%.0f\n"
        "On Ground: %s\n"
        "Facing: %s",
        transform.x, transform.y,
        movement.velocity.x, movement.velocity.y,
        stats.currentHealth, stats.maxHealth,
        stats.isOnGround ? "Yes" : "No",
        movement.isFacingRight ? "Right" : "Left"
    );

    debugLabel->setString(info);
}, 0.1f, "update_debug");
```

## 常见问题

### Q: 玩家不受重力影响？
A: 确保场景使用 `Scene::createWithPhysics()` 并设置了重力。

### Q: 玩家穿墙？
A: 检查物理体的 Bitmask 设置，确保墙壁和玩家可以碰撞。

### Q: 输入没反应？
A: 确保调用了 `PlayerInput::getInstance().initialize(scene)` 和 `update(dt)`。

### Q: 血条/魔法条不显示？
A: `PlayerFactory` 会自动创建UI，检查是否正确传入了 `parentNode`。

### Q: 如何禁用物理引擎？
A: 不调用 `addPhysicsBody(sprite)` 即可，但需要手动处理移动和碰撞。

## 完整 API 参考

查看以下文件：
- `PlayerComponents.h` - 所有组件定义
- `PlayerInput.h` - 输入系统API
- `PlayerFactory.h` - 工厂方法
- `PlayerSystemDesign.md` - 完整设计文档
