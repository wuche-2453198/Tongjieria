Classes\items\Inventory.cpp
对应功能：
以下是排版整理后的方法列表，按照逻辑清晰和易读性进行排版：

---

### Inventory.cpp
1. **getInstance()**
   获取背包单例，首调会构造并初始化默认容量。
2. **init(size_t capacity = 50)**
   重置并设置容量，清空所有槽位并派发刷新事件。
3. **addItem(int id, int count)**
   尝试按 `maxStack` 堆叠/找空格放入物品，返回是否全部放入；成功/失败都会派发刷新事件。
4. **addItemWithOverflow(int id, int count)**
   同 `addItem`，但返回未能放入的剩余部分（`InventorySlot` 列表）用于生成掉落，仍会派发刷新事件。
5. **removeItem(int id, int count)**
   按槽位遍历扣除指定数量，数量不足返回 `false`；变更后派发刷新事件。
6. **swapSlots(size_t a, size_t b)**
   交换两个槽位的内容，索引越界返回 `false`，变更后派发刷新事件。
7. **moveItem(size_t from, size_t to, int count)**
   从一个槽移动部分/全部到另一个槽；
   同 `ID` 自动堆叠并遵守 `maxStack`，不同 `ID` 直接交换；
   支持空槽，越界/无物品返回 `false`；成功后派发刷新事件。
8. **getFirstEmptySlot() const**
   返回首个空槽下标，满则返回 `-1`。
9. **getItemCount(int id) const**
   汇总所有槽中某物品的总数量。
10. **getSlots() const**
    只读访问当前槽位数组。
11. **clear()**
    清空所有槽位并派发刷新事件。
---
#### 内部辅助方法
1. **addItemInternal(int id, int count)**
   实际执行堆叠/填空，返回未放入的数量。
2. **dispatchInventoryChanged()**
   通过 `Event_InventoryChanged` 通知 UI/系统刷新。
---
这种结构清晰地展示了各个方法的功能和关系，便于快速查阅。

### InventoryDef.h
Classes/items/InventoryDef.h 定义了物品与背包槽的基础数据结构：
  - enum class ItemType { Unknown, Weapon, Tool, Material, Consumable, Placeable }: 物品类别枚举。
  - struct ItemDefinition: 静态物品定义（从 JSON 加载），字段含 id、name、type、maxStack、iconPath、value、tags（用于分组/配方模糊匹配）。
  - struct InventorySlot: 运行时背包槽，字段含 itemId（0 表示空）、count、prefixId（预留词缀/附魔）
**此处需要重构物品的类别枚举改为Equipment Materials Placeables Consumables**

### InventoryLayer.h
目前已经具备了鼠标的交互功能（但是现在还没有测试）
### ItemManager.cpp

### 后续改进：1210
InventoryDef中需要重构物品的类别枚举改为Equipment Materials Placeables Consumables
ItemManager---json文件名需要重新对应
InventoryLayer---UI设计中需要：
1.物品栏占整个屏幕的1/4，集中在屏幕的左上角
2.原来的50个物品栏新增4个钱币和弹药栏位，1 个丢弃栏，还有4个武器栏
### 后续改进：1212
链接资源在此处D:\test\Terraria_tj\Resources\items
我现在需要重新构造物品部分的js，生成的js文件放在D:\test\Terraria_tj\Resources\items\items_json目录下
格式要求如下：
ID系统编号：
1000+为装备 里面对应的子目录则类似这样Resources\items\Equipment\中的armor文件1100+，tools1200+
2000+可放制物
3000+为材料
4000+消耗品
优先级从上到下
格式如下
`ID` (int): 唯一标识符。
    * `Name` (string): 显示名称。
    * `Type` (enum): 武器、工具、材料、消耗品、放置物。
    * `MaxStack` (int): 最大堆叠数（如材料999，武器1）。
    * `IconPath` (string): 图片资源路径。
    * `Value` (int): 售价。
    * `Tags/Groups` (list):
#### UI提示词与当前布局说明
**当前布局（已修复）：**
```
位置布局示意图：
[1] [2] [3] [4] [5] [6] [7] [8] [9] [0]  ← 第0行：武器/快捷栏（10个，热键1-0显示在格子上方）
[  ][  ][  ][  ][  ][  ][  ][  ][  ][  ]  [A1][Cu] ← 第1行：普通背包+钱币栏
[  ][  ][  ][  ][  ][  ][  ][  ][  ][  ]  [A2][Ag] ← 第2行：普通背包+钱币栏
[  ][  ][  ][  ][  ][  ][  ][  ][  ][  ]  [A3][Au] ← 第3行：普通背包+钱币栏
[  ][  ][  ][  ][  ][  ][  ][  ][  ][  ]  [A4][Pt] ← 第4行：普通背包+钱币栏
                                     [X]  ← 垃圾桶
```

**详细说明：**
- **武器/快捷栏（索引40-49）**：位于第0行最顶部，10个格子横向排列，尺寸为 slotSize * 1.1，热键标签1-9,0显示在格子上方
- **普通背包（索引0-39）**：10列 x 4行 = 40个格子，位于第1-4行，标准尺寸
- **装备栏（索引50-53）**：4个格子，位于右侧第1行高度，横向排列，标签A1-A4
- **钱币栏（索引54-57）**：4个格子，位于装备栏右边，垂直排列，标签Cu/Ag/Au/Pt（铜/银/金/铂金）
- **垃圾桶（索引58）**：1个格子，位于钱币栏下方，标签X

**实现文件：** Classes/items/InventoryLayer.cpp
- `getSlotPos()` 方法：计算各个栏位的精确位置
- `buildSlots()` 方法：创建所有栏位并设置标签
- 面板位于屏幕左上角，占屏幕约1/4大小

### 关于物品栏后续改进部分
按下Esc或物品栏可以打开物品栏。它由 40 个物品栏栏，10 个热键栏栏，4 个钱币和弹药栏位，1 个丢弃栏
可以使用光标移动物品栏中的物品。在光标停留在某个物品上时按左键会将其从栏位中取出，并附着在光标上。再次点击另一个栏位会将物品，如果那个栏位已经有物品了，则会交换。
将物品移出物品栏界面允许你像在热键栏栏里一样⚒ 使用/攻击它；这可以用来使用物品，例如喝药水。按下⚷ 打开/激活可以将物品丢出。在多人模式中，这可以用于把物品转移给另一个玩家。
某些物品栏栏以一种特殊的方式起作用。
弹药栏只能储存用于远程武器的弹药，用于机械的电线或用于钓竿的鱼饵。请记住，最上方的栏位将首先被用作弹药，依顺序向下。
钱币栏只能储存钱币，且可能会在你死后掉落。当你收集钱币时，它们会自动装入钱币栏，并会在适当时自动转换为下一个更高面额的钱币

物品格子往上提：把物品格子的起始位置调整到面板的上方，确保上面没有空白。装备栏放右侧，独立成一行：将10个武器格放在右上角，单独成行。4个armor格另起一行在金币栏左侧，丢弃栏在金币栏下方：将丢弃栏（Trash）放在金币栏的正下方，右对齐。
## 合成功能

### 物品ID系统重构（正在搭建ing）
种子json
工具json
制作台json
锭json
常见方块json

文件夹整理
装备、材料、消耗品、放置物。
后续添加:木材类（常见木材，以及木材来合成出来的衍生物（木质平台，门，方块等
ID系统编号：
1000+为装备 里面对应的子目录则类似这样Resources\items\Equipment\中的armor文件1100+，tools1200+
2000+可放制物
3000+为材料
4000+消耗品
优先级从上到下
Equipment Materials Placeables Consumables
Placeables
wooden_block (内容包括木质构成的方块)
wooden_item(内容包括由木头为主所构建出来的延伸方块)
material()
Consumables
Ammo（用于存储箭、子弹、飞刀/手里剑、飞镖，炸弹等

### 合成功能UI设计提示词

